#!/usr/bin/python
def validate_password(password):
    for i in password:
        if any(i.isupper()):
            if any(i.isspace):
                if any(i.islower)
                    if i > 7:
                        return True
        else:
            return False